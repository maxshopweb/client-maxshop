export const FILTER_DEBOUNCE_DELAYS = {
  SEARCH: 300, // Reducido para mejor UX
  PRICE: 50, // Reducido para respuesta casi instantánea
} as const;

export const FILTER_LIMITS = {
  CATEGORIAS: 10,
} as const;

